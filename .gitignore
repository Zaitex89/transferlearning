# --- Trained models (regenerate with `python train.py`) ---------------------
# outputs/flower_model.keras is ~52 MB, over GitHub's 50 MB warning threshold.
*.keras
*.h5
*.hdf5
*.pb
*.tflite
*.onnx
saved_model/
checkpoints/

# --- Datasets ---------------------------------------------------------------
# tf_flowers caches in ~/.keras/datasets, but ignore local copies / your own data.
flower_photos/
flower_photos*.tgz
data/
datasets/
my_images/

# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
.pytest_cache/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/
ENV/

# --- Editors / OS -----------------------------------------------------------
.vscode/
.idea/
*.swp
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store

# --- Misc -------------------------------------------------------------------
*.log
.env
.env.local

# Run artifacts — every file in here is recreated by `python train.py`
# (model, both plots, sample grid, metrics.json, train_log.txt).
outputs/